import React from "react"
import { Link } from "gatsby"
import Layout from "../components/Layout/Layout"

export default () => (
  <Layout>
    <div>
      <h1>Contact me here: contact@email.com</h1>
      <p>Let's chat</p>
    </div>
  </Layout>
)
